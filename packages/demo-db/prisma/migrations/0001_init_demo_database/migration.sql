-- =====================================================================
-- Demo Fintech — migration 0001 (infrastructure only)
-- =====================================================================
-- Target-owned migration history starts here. Phase 1 deliberately
-- creates NO domain tables (customers, wallets, payments, ledger —
-- Phase 2). This migration only records the establishment of the
-- target-owned database itself.

SELECT 1;
