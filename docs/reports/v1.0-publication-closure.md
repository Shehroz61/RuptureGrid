# RuptureGrid v1.0 — Publication Closure Report

Date: 2026-09-25
Branch: `v1.0-publication-closure` (created from `phase-11-accepted`)
Starting commit: `61cf203` (`phase-11-accepted` = 61cf20382db3dea7ca51c5131c8758e382e13f52), starting tree clean
Scope: owner-policy and release-facing closure only — no product features, no
business-semantics changes, no migration changes, no protocol-version changes,
no commit, no tag, no push, no v1.0.0 tag created in this pass.

## 1. License decision (owner)

- **License: Apache-2.0** — applied in this pass.
- `LICENSE` added containing the official Apache License Version 2.0 text,
  verbatim, unmodified, with **no invented copyright statement prepended**
  (the license appendix's bracketed placeholder text is part of the
  canonical text and remains as-is).
- `license` metadata: root `package.json` and the three manifests that
  carried `UNLICENSED` (`packages/controlled-faults`, `packages/incident-zero`,
  `packages/showcase`) now declare `"license": "Apache-2.0"`. Thirteen
  workspace manifests never had a license field; they were left untouched —
  every workspace is `private: true` and v1.0 ships no packages.
- SPDX: `Apache-2.0` is the correct SPDX identifier and is what the metadata
  now declares. Project licensing and dependency licensing are kept distinct
  (see `THIRD_PARTY_NOTICES.md`).
- Historical audit reports that truthfully recorded "license undecided" /
  `UNLICENSED` as a pending decision were **not rewritten** — they remain
  accurate history; current docs supersede them.

## 2. Metadata changes (exact)

- `package.json` — `license: UNLICENSED` → `Apache-2.0`. `version` was and
  remains `1.0.0`, `private: true` remains. **No** `repository`, `homepage`,
  or `bugs` URL added (no real public URL exists; none invented).
- `packages/controlled-faults/package.json`, `packages/incident-zero/package.json`,
  `packages/showcase/package.json` — `license: UNLICENSED` → `Apache-2.0`.
- `README.md` — status line updated (phases 0–11 accepted; prepared for public
  source release; source-only; Apache-2.0; remaining owner actions tracked in
  the release checklist); **License section** added. No fake URLs, no
  production-readiness overclaims, no npm publication instructions.
- `SECURITY.md` — rewritten to the owner-decided reporting policy (below).
- `CONTRIBUTING.md` — Apache-2.0 statement and contribution-licensing note
  added; no CLA invented.
- `CHANGELOG.md` — 1.0.0 heading now reads "prepared for v1.0.0 release"
  (not published); a "Release policy (publication closure)" section added;
  known-limitations pointer updated to the release checklist. No fake release
  URL; version headings still carry no link targets.
- **New:** `LICENSE`, `THIRD_PARTY_NOTICES.md`, `docs/reports/release-checklist.md`.

## 3. Security reporting policy

- Intended public channel: **GitHub Private Vulnerability Reporting** on the
  public repository, **to be enabled before public publication**.
- SECURITY.md states explicitly that the channel is **not verified as enabled
  yet** — the public repository does not exist. Nothing claims enablement.
- **No security email exists or is invented**; no fake address, no
  `security@example.com`.
- Public issues must not contain exploit details or secrets (stated).
- Controlled fault injection remains **LOCAL_DEVELOPMENT only**; production
  fault targeting denied server-side (stated).
- DNS rebinding / connection-time TOCTOU residual documented honestly;
  evidence integrity described as detection-oriented, **not** legal
  non-repudiation (records are logically append-only, not immutable).

## 4. Source-only distribution policy

Documented in README, CHANGELOG, SECURITY.md context, and
`THIRD_PARTY_NOTICES.md`: the v1.0.0 release consists of **source,
documentation, migrations, tests, and verifier/showcase source only**. It
does **not** include npm package publication, Docker image publication,
prebuilt application binaries, bundled FFmpeg binaries, or generated showcase
media. All monorepo workspaces remain `private: true` (verified: all 17
workspace manifests). CI remains read-only (`permissions: contents: read`);
no publish workflow, no release token.

## 5. ffmpeg policy

`ffmpeg-static` (GPL-3.0-or-later per dependency metadata) is a
development/showcase dependency used only for the optional showcase video.
The v1 source release does **not** bundle or distribute any FFmpeg binary as a
RuptureGrid artifact. `THIRD_PARTY_NOTICES.md` records this policy and states
dependency licensing facts without legal conclusions; full third-party
licenses are not copied into the repo.

## 6. Scans

**Placeholder scan** (`example.com`, `repo-url-placeholder`, `your-org`,
`your-name`, `security@example`, `TODO-URL`, `TBD`): the only hits are
historical records inside `docs/reports/phase-11-*` documenting the previously
corrected placeholder-URL defect — truthful audit history, preserved. **Zero
hits in active release metadata.**

**License consistency scan** (`UNLICENSED`, `MIT`, `Apache`, `GPL`,
`license`, `copyright`): `UNLICENSED` survives only in the two historical
phase-11 audit reports (as recorded history); no active manifest carries it.
No invented copyright holder exists anywhere in the tree. `GPL` appears only
as the factual `ffmpeg-static` note.

**Secret scan** (private keys, `ghp_`/`github_pat_`/Slack/AWS/OpenAI-style
tokens, JWTs, URL-embedded credentials, personal paths, drive letters): no
secrets found. The only credential-bearing content is the documented
non-production development set (`.env.example`, `compose.yaml`, CI dummy
values — `*_dev_password`, `demo-*-dev-token-0001`), clearly marked
non-production. The only personal-path hit is the Phase 9 audit's own
historical negative-scan record. `.env`, logs, `.artifacts`, browser
profiles: untracked and gitignored (verified `git check-ignore`).

## 7. Frozen install

`pnpm install --frozen-lockfile`: **exit 0**. Lockfile SHA-256
`eb28f309633a4f3b3231da5a7eb2ab4fe45b369b8329942b77e67bfff32c80c8`
**identical before and after** (and equal to the Phase 11 audit record).
Reason: pnpm lockfiles record resolved dependency versions/integrity, not
workspace license fields — license-only metadata changes cannot move it, and
none did.

## 8. Full technical regression (all observed this session)

| Gate | Result |
|---|---|
| `pnpm format:check` | pass (exit 0) |
| `pnpm lint` (workspace + root tests) | pass (exit 0) |
| `pnpm typecheck` (workspace + both test-check programs) | pass (exit 0) |
| `pnpm test:unit` | **33 files / 319 tests passed**, 0 failed |
| `pnpm build` | pass (exit 0) |
| `pnpm test:integration` (real PostgreSQL ×2, Redis, HTTP, browser) | **23 files / 152 tests passed**, 0 failed |
| `pnpm verify` (composite) | pass (exit 0) |
| `pnpm release:verify` (single continuous run) | **PASS (exit 0)** |
| `pnpm incident-zero:verify` | **RESULT: PASS (exit 0)** |
| `pnpm controlled-faults:verify` | PASS: 2× PRE_MUTATION_REJECTION 18/18 assertions, 2× RESPONSE_LOSS 22/22 assertions, leakage: none |
| `pnpm showcase:generate` | verifier exit 0; VULNERABLE run INV-IZ-1 FAIL with 1 finding; SECURE run INV-IZ-1 PASS with no failure findings; 10 screenshots; validation pass |

Showcase video was not re-recorded: publication changes are license/docs
metadata only; no showcase or runtime dependency changed. `showcase:generate`
itself was run and passed; video artifacts remain locally generated,
gitignored, never committed.

## 9. Source archive proof

Temporary tar built from the final tree's tracked + new non-ignored files
(`git ls-files -co --exclude-standard`; 305 files; ~2.5 MB), inspected, then
**deleted**.

- **Included (verified by listing):** `LICENSE`, `README.md`, `CHANGELOG.md`,
  `SECURITY.md`, `CONTRIBUTING.md`, `THIRD_PARTY_NOTICES.md`, full source
  (`packages/` 146 files, `apps/` 56 files), migrations (11 files under
  `prisma/migrations`), tests (32 files under `tests/`), CI
  (`.github/workflows/ci.yml`), root tooling/config, docs (49 files).
- **Excluded (verified absent):** `.env` (only `.env.example` present),
  `node_modules`, `dist`, `.next`, `.artifacts`, `.png`, `.mp4`, `*.log`,
  DB files (`.db`/`.sqlite`), browser profiles, `coverage` — zero matches.

## 10. Remaining owner / publication actions

Full list with order: [docs/reports/release-checklist.md](release-checklist.md).

- **Requires the real public repository:** create/confirm the public GitHub
  repository; enable **GitHub Private Vulnerability Reporting** before
  publication; insert the real URL into `repository`/`homepage` metadata only
  once it exists.
- **Optional owner metadata:** copyright holder (deliberately not invented;
  the Apache-2.0 LICENSE file is included without an owner identity line).
- **Then:** final release verification, clean tree, `v1.0.0` annotated tag,
  push only after human approval, optional source-only GitHub release.

## 11. Publication readiness state (not blurred)

- **TECHNICAL PUBLICATION CLOSURE: READY FOR FINAL RELEASE AUDIT.**
- **ACTUAL PUBLICATION: `PUBLISH NOW` = FALSE** — blocked until the real
  public repository exists, the real URL (if metadata should expose one) is
  inserted, and Private Vulnerability Reporting is enabled or another real
  vulnerability channel is supplied.

## 12. Phase preservation

No accepted migration changed (zero migration files in the diff; zero code
files in the diff). All phase tags verified intact after the change set
(`phase-0-accepted` … `phase-11-accepted`; `phase-11-accepted` still = `61cf203`).
No history rewritten; the closure branch was created, nothing committed.
