import type { NextConfig } from 'next';

// Phase 1: engineering foundation only. No product features, no
// analytics, no external integrations. Defaults are intentional.
const nextConfig: NextConfig = {};

export default nextConfig;
