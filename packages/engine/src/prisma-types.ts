// =====================================================================
// Re-exports of the generated Control client model types used by the
// engine. Keeps engine modules decoupled from the generated path.
// =====================================================================

export type {
  TargetRegistrationModel as TargetRegistration,
  TargetOriginModel as TargetOrigin,
  ExperimentDefinitionModel as ExperimentDefinition,
  ExperimentRevisionModel as ExperimentRevision,
  RunSnapshotModel as RunSnapshot,
  ExperimentRunModel as ExperimentRun,
  ExperimentStepRunModel as ExperimentStepRun,
  StepInvocationModel as StepInvocation,
  StaleWriterEventModel as StaleWriterEvent,
} from '@rupturegrid/control-db';
